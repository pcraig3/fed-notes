<header class="intro">
  <div class="container">
    <h1>{{ introHeading }}</h1>
    {% if introSummary %}
      <div class="introSummary">{{ introSummary | safe }}</div>
    {% endif %}
  </div>
</header>
